import { Authentication } from '../../../lib/_Authentication/entities';

export const mockedAuthentication: Authentication = {
  success: true,
  message: 'Your are granted',
  userId: 'usr-nbw2s2nkks9wmmw2kk',
};
